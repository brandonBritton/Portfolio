import logo from '../assets/logo.svg';

const Nav = () => {

    return (
        <nav>
            <div>
                <img src={ logo } alt='logo' />
                <p>
                    Brandon.
                </p>
            </div>
        </nav>
    );
};

export default Nav;